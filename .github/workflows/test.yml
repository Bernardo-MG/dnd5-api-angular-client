name: Testing and validation

on: [push, pull_request]

jobs:
  tests:
    name: Tests with node 19
    uses: Bernardo-MG/github-workflow-angular/.github/workflows/testing.yml@v1
    with:
      node: 19
